const correctAnswers = ["A", "B", "D", "C", "A"];

const jimmyAnswer = ["A", "B", "A", "B", "A"];
const shellyAnswer = ["C", "C", "C", "C", "C"];

function getScore(studentAnswer) {
  // Your code
  return 100;
}

console.log(getScore(jimmyAnswer)); // should print 60
console.log(getScore(shellyAnswer)); // should print 20
